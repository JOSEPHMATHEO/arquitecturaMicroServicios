# LumenLibrariesApi

Microservice to manage physical libraries and their collections.

Run:

1. `composer install` (inside the folder)
2. `php artisan migrate`
3. `php artisan db:seed`
4. `php -S localhost:8017 -t public`
